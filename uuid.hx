package com.artemis.utils

/**
 *
 * The entity class.
 *  inspired partially by Robert Kieffer's Math.uuid.js
 *  ported to HaXe by Lewen Yu
 *
 */


class Uuid {

    //fast UUID generation based on RFC4122v4
    public static function getUUID( ?seed : Int ) {
        if ( seed.isNull() ) {
            seed = //random seed
        }
    }
}
